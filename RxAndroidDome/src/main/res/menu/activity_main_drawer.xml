<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Observables组合">
        <menu>
            <item
                android:id="@+id/item_merge"
                android:icon="@drawable/ic_menu_send"
                android:title="Merge" />
            <item
                android:id="@+id/item_zip"
                android:icon="@drawable/ic_menu_send"
                android:title="ZIP" />
            <item
                android:id="@+id/item_join"
                android:icon="@drawable/ic_menu_send"
                android:title="Join"
                />
            <item
                android:id="@+id/item_combineLatest"
                android:icon="@drawable/ic_menu_send"
                android:title="CombineLatest"
                />
            <item
                android:id="@+id/item_andTheWhen"
                android:icon="@drawable/ic_menu_send"
                android:title="AndTheWhen"
                />
        </menu>
    </item>
    <item android:title="Schedulers">
        <menu>
            <item
                android:id="@+id/item_down"
                android:icon="@drawable/ic_menu_send"
                android:title="downLoadFile"
                />
        </menu>
    </item>

</menu>
